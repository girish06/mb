<div class="forgot-pass-mail-link">
    <form{{attributes}}>
        <div class="dontdisplay">
            {{children}}
        </div>

        {{element.account.pass}}
        {{element.actions.submit}}
        {{ element.form_build_id }}
        {{ element.form_id }}
    </form>
    <p>Do not have an account? <a class="sign-up"  href='/user/register'>Sign Up Now</a></p>
</div>
